# Production Profile Configuration
# Optimized for production with security and performance

spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          batch_size: 50
    open-in-view: false

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true

# AI Configuration for Production
ai:
  provider:
    openai:
      api-key: ${OPENAI_API_KEY}
      model: ${OPENAI_MODEL:gpt-4o-mini}
      embedding-model: ${OPENAI_EMBEDDING_MODEL:text-embedding-3-small}
    pinecone:
      api-key: ${PINECONE_API_KEY}
      environment: ${PINECONE_ENVIRONMENT:production}
      index-name: ${PINECONE_INDEX_NAME:easyluxury-prod}
  
  # Vector Database Configuration for Production
  vector-db:
    type: ${AI_VECTOR_DB_TYPE:pinecone}  # Use Pinecone for production
    lucene:
      index-path: ${AI_LUCENE_INDEX_PATH:/var/lib/easyluxury/lucene-vector-index}
      similarity-threshold: ${AI_LUCENE_SIMILARITY_THRESHOLD:0.7}
      max-results: ${AI_LUCENE_MAX_RESULTS:100}
      create-index-if-not-exists: true
      analyzer: standard
      use-compound-file: true
      max-buffered-docs: 5000
    pinecone:
      api-key: ${PINECONE_API_KEY}
      environment: ${PINECONE_ENVIRONMENT:production}
      index-name: ${PINECONE_INDEX_NAME:easyluxury-prod}
      dimensions: ${PINECONE_DIMENSIONS:1536}
      metric: cosine
      pods: ${PINECONE_PODS:2}
      pod-type: ${PINECONE_POD_TYPE:p2}
      enable-metadata-filtering: true
  
  service:
    enabled: true
    auto-configuration: true
    caching:
      enabled: true
    metrics:
      enabled: true
    health-checks:
      enabled: true
    logging:
      enabled: true
    default-timeout: 30000
    max-retries: 3
    retry-delay: 1000
    async:
      enabled: true
      thread-pool-size: 20
    batch-processing:
      enabled: true
      batch-size: 200
    rate-limiting:
      enabled: true
      rate-limit-per-minute: 1000
    circuit-breaker:
      enabled: true
      threshold: 5
      timeout: 60000

    indexing:
      enabled: true
      queue:
        max-retries: ${AI_INDEXING_MAX_RETRIES:5}
        visibility-timeout: ${AI_INDEXING_VISIBILITY_TIMEOUT:PT1M}
      async-worker:
        enabled: true
        fixed-delay: ${AI_INDEXING_ASYNC_DELAY:PT0.5S}
        batch-size: ${AI_INDEXING_ASYNC_BATCH:200}
        strategy: ASYNC
      batch-worker:
        enabled: true
        fixed-delay: ${AI_INDEXING_BATCH_DELAY:PT20S}
        batch-size: ${AI_INDEXING_BATCH_SIZE:2000}
        strategy: BATCH
      cleanup:
        enabled: true
        stuck-threshold: ${AI_INDEXING_STUCK_THRESHOLD:PT5M}
        sweep-interval: ${AI_INDEXING_SWEEP_INTERVAL:PT2M}
        completed-retention: ${AI_INDEXING_COMPLETED_RETENTION:P7D}
        dead-letter-retention: ${AI_INDEXING_DEAD_LETTER_RETENTION:P30D}

    cleanup:
      enabled: true
      retention-cron: "0 30 3 * * *"
      orphaned-entities:
        enabled: true
        cron: "0 0 4 * * SUN"
      no-vector-entities:
        enabled: true
        cron: "0 0 5 * * SUN"
        retention-hours: 24
      strategies:
        order: ARCHIVE
        user: ARCHIVE
        product: SOFT_DELETE
        behavior: HARD_DELETE
        analytics: HARD_DELETE
      retention-days:
        order: 2555
        user: 365
        product: 180
        behavior: 90
        analytics: 30
        default: 180

# Easy Luxury AI Configuration for Production
easyluxury:
  ai:
    # Index Names
    product-index-name: ${AI_PRODUCT_INDEX_NAME:easyluxury-products-prod}
    user-index-name: ${AI_USER_INDEX_NAME:easyluxury-users-prod}
    order-index-name: ${AI_ORDER_INDEX_NAME:easyluxury-orders-prod}
    
    # Feature Toggles - Environment controlled
    enable-product-recommendations: ${AI_ENABLE_PRODUCT_RECOMMENDATIONS:true}
    enable-user-behavior-tracking: ${AI_ENABLE_USER_BEHAVIOR_TRACKING:true}
    enable-smart-validation: ${AI_ENABLE_SMART_VALIDATION:true}
    enable-ai-content-generation: ${AI_ENABLE_CONTENT_GENERATION:true}
    enable-ai-search: ${AI_ENABLE_SEARCH:true}
    enable-ai-rag: ${AI_ENABLE_RAG:true}
    enable-product-description-generation: ${AI_ENABLE_PRODUCT_DESCRIPTION:true}
    enable-product-categorization: ${AI_ENABLE_PRODUCT_CATEGORIZATION:true}
    enable-product-tagging: ${AI_ENABLE_PRODUCT_TAGGING:true}
    
    # AI Model Configuration
    default-ai-model: ${AI_DEFAULT_MODEL:gpt-4o-mini}
    default-embedding-model: ${AI_DEFAULT_EMBEDDING_MODEL:text-embedding-3-small}
    max-tokens: ${AI_MAX_TOKENS:2000}
    temperature: ${AI_TEMPERATURE:0.3}
    timeout-seconds: ${AI_TIMEOUT_SECONDS:60}
    
    # Product-Specific AI Settings
    product:
      description-generation:
        enabled: ${AI_PRODUCT_DESCRIPTION_ENABLED:true}
        max-length: ${AI_PRODUCT_DESCRIPTION_MAX_LENGTH:500}
        style: ${AI_PRODUCT_DESCRIPTION_STYLE:luxury}
      categorization:
        enabled: ${AI_PRODUCT_CATEGORIZATION_ENABLED:true}
        categories: ${AI_PRODUCT_CATEGORIES:luxury,premium,exclusive,limited-edition}
      tagging:
        enabled: ${AI_PRODUCT_TAGGING_ENABLED:true}
        max-tags: ${AI_PRODUCT_MAX_TAGS:10}
        tag-types: ${AI_PRODUCT_TAG_TYPES:material,style,occasion,price-range,season}
      search:
        enabled: ${AI_PRODUCT_SEARCH_ENABLED:true}
        similarity-threshold: ${AI_PRODUCT_SIMILARITY_THRESHOLD:0.7}
        max-results: ${AI_PRODUCT_MAX_SEARCH_RESULTS:20}
      recommendations:
        enabled: ${AI_PRODUCT_RECOMMENDATIONS_ENABLED:true}
        max-recommendations: ${AI_PRODUCT_MAX_RECOMMENDATIONS:10}
        algorithm: ${AI_PRODUCT_RECOMMENDATION_ALGORITHM:collaborative-filtering}
    
    # User-Specific AI Settings
    user:
      behavior-tracking:
        enabled: ${AI_USER_BEHAVIOR_TRACKING_ENABLED:true}
        track-views: ${AI_USER_TRACK_VIEWS:true}
        track-purchases: ${AI_USER_TRACK_PURCHASES:true}
        track-searches: ${AI_USER_TRACK_SEARCHES:true}
        retention-days: ${AI_USER_BEHAVIOR_RETENTION_DAYS:365}
      preferences:
        learning-enabled: ${AI_USER_PREFERENCES_LEARNING:true}
        update-frequency: ${AI_USER_PREFERENCES_UPDATE_FREQUENCY:daily}
        min-interactions: ${AI_USER_MIN_INTERACTIONS:5}
      recommendations:
        enabled: ${AI_USER_RECOMMENDATIONS_ENABLED:true}
        max-recommendations: ${AI_USER_MAX_RECOMMENDATIONS:15}
        diversity-factor: ${AI_USER_RECOMMENDATION_DIVERSITY:0.3}
    
    # Order-Specific AI Settings
    order:
      analysis:
        enabled: ${AI_ORDER_ANALYSIS_ENABLED:true}
        pattern-recognition: ${AI_ORDER_PATTERN_RECOGNITION:true}
        fraud-detection: ${AI_ORDER_FRAUD_DETECTION:true}
      insights:
        enabled: ${AI_ORDER_INSIGHTS_ENABLED:true}
        trend-analysis: ${AI_ORDER_TREND_ANALYSIS:true}
        seasonal-patterns: ${AI_ORDER_SEASONAL_PATTERNS:true}

# Supabase Configuration for Production
supabase:
  url: ${SUPABASE_URL}
  api-key: ${SUPABASE_API_KEY}
  service-role-key: ${SUPABASE_SERVICE_ROLE_KEY}

# Mock Authentication Configuration for Production
app:
  mock-auth:
    enabled: false

# AWS S3 Configuration for Production
aws:
  s3:
    endpoint: ${AWS_S3_ENDPOINT:https://s3.amazonaws.com}
    region: ${AWS_S3_REGION:us-east-1}
    bucket: ${AWS_S3_BUCKET}
    access-key: ${AWS_ACCESS_KEY_ID}
    secret-key: ${AWS_SECRET_ACCESS_KEY}
    presigned-url-duration: ${AWS_PRESIGNED_URL_DURATION:3600}

# Logging for Production
logging:
  level:
    root: WARN
    com.easyluxury: INFO
    com.ai.infrastructure: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: /var/log/easyluxury/application.log
    max-size: 100MB
    max-history: 30

# Management/Actuator for Production
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true